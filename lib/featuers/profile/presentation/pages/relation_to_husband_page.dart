import '../widgets/barrel.dart';

class RelationToHusbandPage extends StatelessWidget {
  const RelationToHusbandPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: ColorManager.scaffoldSecondBackGround,
      appBar: CustomAppBar(title: "Relation to husband"),
    );
  }
}
